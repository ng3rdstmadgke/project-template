# AIの行動原則

## 禁止事項

以下の行為は一切禁止とする

- 本番環境の**変更**・**デプロイ**
- 許可なくリモートリポジトリにpushすること
- ユーザーの質問・指摘に対して、方針の合意を取らずにファイルを編集すること

## 行動規範

### 1. ユーザーへの応答

- ユーザーには日本語で応答すること
- ユーザーの質問には簡潔に回答すること。詳細な説明や補足が必要な場合はこちらから要求する。聞かれていないことを長々と説明しない。
- 回答が長くなる場合は「結論・サマリ」を最初に示し、詳細の説明はその後に述べること
- 質問・指摘を受けた際に、ユーザーとの方針の合意なしにソースコードを修正しないこと。回答するのみにとどめること
- サービスやライブラリの仕様・設定・使い方はcontext7(mcp)やweb検索で必ず公式の最新情報の裏取りを行うこと

### 2. タスク戦略

- 問題が発生した場合は、無理に押し進めず、ただちに停止してユーザーに計画の立て直しを相談すること
- 正しく動作していることを確認せずにタスクを完了とマークしてはならない。テストを実行し、ログを確認し、正確性を実証すること。
- 調査、探索、分析など独立して実行可能なタスクは sub agent に依頼する

### 3. 設計の基本原則

- **シンプル第一**: YAGNIを徹底すること。必要な箇所だけに手を加える。
- **怠慢の禁止**: 現在の実装に引きずられたり、一時しのぎの修正を行わない。問題の表面的な解決を目指すのではなく、根本原因を設計レベルで解決すること。
- **開放閉鎖の原則(OCP)**: ソフトウェアの構成要素は拡張に対して開かれており、変更に対して閉じていなければならない。長期的な運用・拡張が容易なアーキテクチャとすること。
- **普遍的な設計原則への準拠**: Clean Architecture, DDD, TDD, SOLIDの原則, GoFデザインパターンといった普遍的な設計原則に準拠すること

### 4. 自律的な調査

- 調査依頼を受けたら、ログ、ソースコード、CLI、MCP、Web検索を適切に使い分け、原因究明と修正方針の策定までを自律的に行うこと。ただし、環境に変更を加えてはならない

# プロジェクト概要

## 目的

このリポジトリは、新たな開発プロジェクトを始める際のテンプレートとなるリポジトリである。  
devcontainerのセットアップや、開発ツール・CLI、言語、AI用の各種スキル・MCPサーバー設定を含む。  

## 開発環境

本環境はDevContainerであり、開発、デプロイはすべてDevContainer上で行う。

- DevContainerの定義: `.devcontainer/devcontainer.json`
- DevContainer上に定義されている環境変数:
  - `.devcontainer/devcontainer.json` の `containerEnv` : devcontainerのプロパティを参照する必要がある環境変数の定義はここ。なるべくスリムに保つ
  - `.devcontainer/.env` : 機密情報などcommitしたくない変数はここに定義
  - `.devcontainer/.private.bashrc` : 一般に環境変数を定義するのはここ。`source ~/.bashrc` で読み込まれる
- Devcontainerのコンテナイメージ: `.devcontainer/Dockerfile`

## チケットとソース管理について

- ソースコード・チケット(issue)は https://github.com/ng3rdstmadgke/project-template リポジトリで管理される
- ソースコード、チケット(issue)、マージリクエストなどへのアクセスは `gh` コマンドを利用すること
- ブランチ名には `feature/チケットID_xxxxxxxxx` のようなルールでチケットIDが含まれる。ブランチに対応するチケットを探すときはこのチケットIDを使うこと。
- コミットメッセージは原則 `ref #チケットID メッセージ` という形式になっている。チケットIDでチケットにアクセスすることでコミットの背景や経緯を探ることができる。

## 技術スタック

## アーキテクチャ (要点)

アーキテクチャの正典は `docs/architecture/orverview.md` である。
構成、ファイルの対応関係、設計に関わる作業を始める前に必ず読み、アーキテクチャの全体像を把握すること。

## テスト (要点)

## ドキュメント

### 分類基準

- 規範(こうせよ、こうしてはならない)は `CLAUDE.md` にかく。もしくは `.claude/skills/` 配下にスキル化する
- 記述(こうなっている、こうやる) は `docs/` に書く


### ドキュメント構成

ドキュメント構成は [Diátaxis](https://diataxis.fr/) をベースに以下の構成とする

| ディレクトリ | 目的 | 更新責務 | 補足 |
| :--- | :--- | :--- | :--- |
| `docs/how-to/` | 特定の目的の達成を支援する | 常に最新に保つ | 主にデプロイ、運用オペレーション、開発フローなどの手順書 |
| `docs/architecture/` | 設計を理解する。 | 常に最新に保つ | 通読するものではなく、必要なときに特定の章を参照するもの。説明は章ごとに閉じる。<br>現在の設計のみを説明する。過去の仕様には言及しない。 |
| `docs/reference/` | 情報の参照 | 常に最新に保つ | 手順書や設計ドキュメントに含めるには冗長すぎる補足や外部ライブラリの仕様を説明する。設計
| `docs/background/` | 背景や前提情報を得る | 常に最新に保つ | このプロジェクトで利用する技術要素(ライブラリ,サービス,言語, etc)の一般的な説明やハンズオン |
| `docs/adr/` | 意思決定の履歴を追う | 追記のみ。過去は書き換えない。 | [MADR (Markdown Architectural Decision Records)](https://adr.github.io/madr/) の [minimal template](https://raw.githubusercontent.com/adr/madr/refs/tags/4.0.0/template/adr-template-minimal.md) で記述する<br>ファイル名は `<TICKET_ID>_<TOPIC>.md` とする。 |
| `docs/releases/` | 一度きりの作業手順を記録 | 実施後は凍結 | 恒久的な手順ではなく、大規模な機能開発等によって生じたイレギュラーなデプロイ手順などを記述する。 ファイル名は `YYYY-MM-DD-<TOPIC>.md |

以下のように、サブディレクトリは必要に応じて作成すること。

```
docs/
  how-to/
    development/  # 開発における操作
    operations/   # 稼働環境に対する操作
  architecture/
    overview.md  # 全体のアーキテクチャ
    infra/        # インフラのアーキテクチャ
    app/          # アプリのアーキテクチャ
    cicd/         # CICDのアーキテクチャ
  reference/
  background/
  adr/
  releases/
```

補足:
- 「その他」に相当するディレクトリは作らない
- 画像は参照する文章と同じ階層の `img/` に配置すること。文章を動かすときは `img/` も一緒に動かすこと。

### 執筆規約

- 共通
  - 構造・関係・流れを説明したい場合は Mermaid、箇条書き、表などで視覚化するとよい
  - Mermaidの利用用途
    - `flowchart` : コンポーネント間の関係
    - `sequenceDiagram` : API、deploy、認証などの時系列
    - `stateDiagram-v2` : 状態遷移
    - `erDiagram` : テーブルのリレーション、データモデルの概念説明
    - `classDiagram` : クラス・オブジェクトの依存関係
    - `architecture-beta` : アーキテクチャ図
  - 公式ドキュメントなどリファレンスのリンクは関連する文章の近くに置くこと。最後にまとめて参考文献の一覧としない。
  - なんらかの「説明」を記述する場合、冒頭に数行程度サマリを入れ、その章・項で何を説明するかを素早く把握できるようにすること
- 手順書 (`docs/how-to/`)
  - 「なんの目的で、なにを行う」という指示形式の文章で書くこと
  - 冗長な背景説明や補足は手順書に含めない
- アーキテクチャドキュメント (`docs/architecture/`)
  - 全体像(設計思想、流れ、構造)を把握することを目的とする。
  - 技術的な詳細に踏み込んではいけない。技術的な詳細はソースコードや手順書(`docs/how-to/`)、リファレンス(`docs/reference/`)へのリンクとして逃がす。
